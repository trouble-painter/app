import 'package:flutter/material.dart';

class GameGuide extends StatelessWidget {
  const GameGuide({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Tutorial...");
  }
}
